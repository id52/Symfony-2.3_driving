{% extends 'AppBundle::layout_new.html.twig' %}

{% form_theme form 'AppBundle::form_new.html.twig' %}

{% block content %}
<nav class="path">
    <a href="{{ path('homepage') }}">{{ 'titles.homepage'|trans }}</a>
    » <strong>{{ 'titles.feedback_teacher'|trans }}</strong>
</nav>

<div class="mainContent">
    {% include 'AppBundle::_paid2_counter.html.twig' %}
    {% include 'AppBundle::_owe_stage_paid.html.twig' %}

    <div class="title"><h1>{{ 'titles.feedback_teacher'|trans }}</h1></div>

    <p>Если в процессе обучения у Вас возникли вопросы по изучаемому материалу, Вы можете задать их нашему преподавателю. Для этого выберите предмет, тему предмета и напишите Ваш вопрос.</p>

    <div class="form">
        {{ form_start(form) }}
            <div class="row">
                {{ form_widget(form.destination, { attr: { 'data-placeholder': 'Выберите преподавателя' }}) }}
                {{ form_errors(form.destination) }}
            </div>

            <div class="row">
                {{ form_widget(form.theme, { attr: { 'data-placeholder': 'Выберите тему обращения' }}) }}
                {{ form_errors(form.theme) }}
            </div>

            <div class="row">
                <div class="input">
                    {{ form_widget(form.text, { attr: { placeholder: 'Ваше сообщение' } }) }}
                </div>
                {{ form_errors(form.text) }}
            </div>

            <div class="links bottom">
                <span class="link bg-green message-bg mail-link">
                    Отправить сообщение
                    <input value="" type="submit">
                </span>
            </div>
        {{ form_errors(form) }}
        {{ form_end(form) }}
    </div>
</div>
{% endblock content %}
