{% extends "AppBundle::layout_new.html.twig" %}

{% block content %}
<nav class="path">
    <a href="{{ path('homepage') }}">{{ 'titles.homepage'|trans }}</a>
    » <a href="{{ path('my_trainings') }}">{{ 'titles.trainings'|trans }}</a>
    » <strong>{{ 'final_exam'|trans }}</strong>
</nav>

<div class="mainContent">
    {% include 'AppBundle::_paid2_counter.html.twig' %}
    {% include 'AppBundle::_owe_stage_paid.html.twig' %}

    <div class="title"><h1>{{ settings_notifies['info_attempts_title']|raw }}</h1></div>
    <div class="text">{{ message|raw }}</div>
    <div class="links">
        <a class="bg-red" href="{{ path('my_training_final_exam', {access: 1}) }}">{{ 'exam_start'|trans }}</a>
        <a class="bg-green" href="{{ path('my_trainings') }}">{{ 'training.final_exam.back_to_list'|trans }}</a>
    </div>
</div>
{% endblock content %}
