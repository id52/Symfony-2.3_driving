{% extends "AppBundle::layout_new.html.twig" %}

{% block content %}
{% if article_med  %}
    <nav class="path">
        <a href="{{ path('homepage') }}">{{ 'titles.homepage'|trans }}</a>
        » <strong>{{ article_med.title }}</strong>
    </nav>

    <div class="mainContent">
        {% include 'AppBundle::_paid2_counter.html.twig' %}
        {% include 'AppBundle::_owe_stage_paid.html.twig' %}

        <section>
            <div class="title"><h1>{{ article_med.title }}</h1></div>

            <div class="wid">
                {{ article_med.text|raw }}
            </div>
        </section>
    </div>
{% endif %}
{% endblock content %}
