{% extends "AppBundle::layout_new.html.twig" %}

{% block content %}
<nav class="path">
    <a href="{{ path('homepage') }}">{{ 'titles.homepage'|trans }}</a>
    » <strong>{{ 'titles.trainings'|trans }}</strong>
</nav>

<div class="mainContent">
    {% include 'AppBundle::_paid2_counter.html.twig' %}
    {% include 'AppBundle::_owe_stage_paid.html.twig' %}

    <div class="title">
        <h1>{{ settings_notifies['timelimit_after_1_payment_title'] }}</h1>
    </div>
    {{ settings_notifies['timelimit_after_1_payment_text']|raw }}
    <div class="links">
        <a class="bg-red" href="javascript:window.history.back()">{{ 'buttons.back'|trans }}</a>
        <a class="bg-green" href="{{ path('my_payments') }}">{{ 'buttons.pay'|trans }}</a>
    </div>
</div>
{% endblock content %}
