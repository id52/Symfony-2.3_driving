{% extends 'AppBundle::admin.html.twig' %}

{% block content %}
    <h3>{{ (app.request.get('_route') == router_item_edit ? ('admin.'~entity_name_s~'_edit_title') : (entity_name_s~'_add_title'))|trans }}</h3>
    <hr>

    {{ form_start(form) }}
        {{ form_rest(form) }}

        <div class="form-actions">
            <button type="submit" class="btn btn-success">
                <i class="fa fa-download"></i>
                <span>{{ 'admin.buttons.save'|trans }}</span>
            </button>
            <a class="btn pull-right" href="{{ path(router_list) }}"><i class="fa fa-arrow-left"></i> {{ 'admin.buttons.back_to_list'|trans }}</a>
        </div>
    {{ form_end(form) }}
{% endblock content %}

{% block js %}
    <script src="/ckeditor/ckeditor.js"></script>
{% endblock js %}
