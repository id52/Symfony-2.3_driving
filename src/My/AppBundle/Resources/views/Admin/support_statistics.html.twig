{% extends "AppBundle::admin.html.twig" %}

{% block content %}
    <h3>Статистика</h3>
    <hr>
    <a id="filter_link" href="#"><i class="fa fa-caret-down"></i> Фильтр</a>
    <div id="filter">
        {{ form_start(filter_form) }}
        {{ form_widget(filter_form) }}
        <div class="form-actions">
            <button type="submit" class="btn btn-success pull-right">
                <i class="fa fa-filter"></i>
                <span>{{ 'buttons.filter'|trans }}</span>
            </button>
        </div>
        {{ form_end(filter_form) }}
    </div>
    <div id="filter_default">Данные за {{ form|date('d.m.Y') }} &ndash; {{ to|date('d.m.Y') }}</div>
    <hr>
{#
    <h4>Модераторы</h4>
    {% if data.moderators|length %}
        <table class="table table-hover">
            <tr>
                <th>Модератор</th>
                <th class="min_col">Количество сообщений</th>
            </tr>
            {% for moderator in data.moderators %}
                <tr>
                    <td><a href="{{ path('admin_support_dialogs')~'?support_dialog[user_email]='~moderator.obj.email }}">{{ moderator.obj.first_name }} {{ moderator.obj.last_name }} ({{ moderator.obj.email }})</a></td>
                    <td class="min_col">{{ moderator.messagesCount }}</td>
                </tr>
            {% endfor %}
        </table>
    {% else %}
        Нет сообщений
    {% endif %}
#}

    <h4>Подкатегории</h4>
    {% if data.subCategories|length %}
        <table class="table table-hover">
            <tr>
                <th>Категория</th>
                <th class="min_col">Диалогов всего</th>
                <th class="min_col">Неотвеченных диалогов</th>
            </tr>
            {% for category in data.subCategories %}
                <tr>
                    <td><a href="{{ path('admin_support_dialogs')~'?support_dialog[category_name]='~category.id }}">{{ category.parentName }}: {{ category.name }}</a></td>
                    <td class="min_col">{{ category.dialogsTotalCount }}</td>
                    <td class="min_col"><a href="{{ path('admin_support_dialogs')~'?support_dialog[category_name]='~category.id~'&support_dialog[answered]=no' }}">{{ category.dialogsWithoutAnswerCount }}</a></td>
                </tr>
            {% endfor %}
        </table>
    {% else %}
        Нет категорий
    {% endif %}

{% endblock content %}

{% block js %}
<script>
$(function() {
    {% if app.request.queryString == '' %}
    $('#filter').hide();
    {% else %}
    $('#filter_default').hide();
    {% endif %}
    $('#filter_link').click(function() {
        $('#filter').toggle();
        {% if app.request.queryString == '' %}
        $('#filter_default').toggle();
        {% endif %}
        return false;
    });
});
</script>
{% endblock js %}

