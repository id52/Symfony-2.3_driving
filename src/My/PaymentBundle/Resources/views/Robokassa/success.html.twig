{% extends "AppBundle::layout_new.html.twig" %}

{% block content %}
<nav class="path">
    <a href="{{ path('homepage') }}">{{ 'titles.homepage'|trans }}</a>
    » <strong>{{ 'titles.payments'|trans }}</strong>
</nav>

<div class="e_wrap_payment">
    <div class="e_item_payment--img m_success"></div>
    <div class="e_item_payment--text">
        Оплата успешно завершена
    </div>
</div>
<p class="e_payment_text">
    Как только к нам поступит ваша оплата, в разделе <span>«УВЕДОМЛЕНИЯ»</span> <br>
    слева появится новое сообщение с инструкцией дальнейших действий.
</p>
{% endblock content %}

{% block js %}
<script>
$(function() {
    setInterval(function() {
        $.getJSON('{{ path('robokassa_is_paid') }}', function(data) {
            if (data.is_paid) {
                window.location = data.redirect_url;
            }
        });
    }, 3000);
});
</script>
{% endblock %}
